doctype html
html
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    title New Workshop Registration
    style.
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        background-color: #f0e6ff;
        padding: 20px;
        text-align: center;
        border-radius: 5px 5px 0 0;
      }
      .content {
        background-color: #fff;
        padding: 20px;
        border: 1px solid #ddd;
      }
      .footer {
        background-color: #f5f5f5;
        padding: 15px;
        text-align: center;
        font-size: 12px;
        color: #666;
        border-radius: 0 0 5px 5px;
      }
      h1 {
        color: #722ed1;
        margin: 0;
      }
      table {
        width: 100%;
        border-collapse: collapse;
        margin: 20px 0;
      }
      table, th, td {
        border: 1px solid #ddd;
      }
      th {
        background-color: #f0e6ff;
        padding: 10px;
        text-align: left;
      }
      td {
        padding: 10px;
      }
      .status {
        padding: 5px 8px;
        border-radius: 4px;
        font-weight: bold;
      }
      .status-pending {
        background-color: #faad14;
        color: #fff;
      }
      .status-completed {
        background-color: #52c41a;
        color: #fff;
      }
  body
    div.container
      div.header
        h1 New Workshop Registration
      div.content
        p A new registration has been submitted for Dr. Agarwal's Psychology Workshop.
        
        table
          tr
            th Registration ID
            td= registration.id
          tr
            th Name
            td= registration.name
          tr
            th Email
            td= registration.email
          tr
            th Phone
            td= registration.phone
          tr
            th Age
            td= registration.age
          tr
            th Interest Area
            td= registration.interestArea
          tr
            th Preferred Dates
            td= registration.preferredDates
          tr
            th Preferred Timing
            td= registration.preferredTiming
          tr
            th Expectations
            td= registration.expectations
          tr
            th Referral Source
            td= registration.referralSource
          tr
            th Payment Status
            td
              span(class=`status ${registration.isCompleted ? 'status-completed' : 'status-pending'}`)= registration.paymentStatus
          tr
            th Registration Date
            td= registration.createdAt
        
        p You can view and manage all registrations in the admin dashboard.
      div.footer
        p This is an automated notification from Dr. Agarwal's Workshop Registration System. 