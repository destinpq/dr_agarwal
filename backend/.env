# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dr_agarwal
DB_USER=postgres
# Password is masked for security
DB_PASSWORD=tiger
DB_SSL_MODE=disable

# Database connection string (constructed from above values)
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=${DB_SSL_MODE}

# Application settings
PORT=8080
NODE_ENV=development
HEALTH_PORT=8081

# Admin password for authentication
ADMIN_PASSWORD=admin123

# Email Configuration - SMTP (for sending emails)
EMAIL_HOST=smtpout.secureserver.net
EMAIL_PORT=465
EMAIL_SECURE=true
EMAIL_USER=support@destinpq.com
EMAIL_PASSWORD=DestinPQ24225
EMAIL_FROM=support@destinpq.com

# Email Configuration - IM
AP (for receiving emails)
IMAP_HOST=imap.godaddy.com
IMAP_PORT=993
IMAP_SECURE=true

# Email Configuration - POP3 (alternative for receiving emails)
POP3_HOST=pop.godaddy.com
POP3_PORT=995
POP3_SECURE=true

# WhatsApp Configuration
# The path where WhatsApp session data is stored (relative to backend directory)
WHATSAPP_SESSION_PATH=./whatsapp-sessions
# Client ID for the WhatsApp instance
WHATSAPP_CLIENT_ID=dr-agarwal-workshop
# Optional timeout in milliseconds for WhatsApp operations (default: 60000)
WHATSAPP_TIMEOUT=120000
# WhatsApp phone number (with country code)
WHATSAPP_PHONE_NUMBER=919873521968
# Default country code for phone numbers without one
WHATSAPP_DEFAULT_COUNTRY_CODE=91 